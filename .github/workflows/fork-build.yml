name: fork-build
on:
  workflow_dispatch:
    inputs:
      target:
        description: "Build target platform"
        required: true
        type: choice
        options:
          - macos-arm64
          - windows-x64
          - both
        default: both

jobs:
  build-cli-macos:
    runs-on: macos-latest
    if: inputs.target == 'both' || inputs.target == 'macos-arm64'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install

      - name: Download DuckDB CLI for macOS
        run: |
          cd packages/opencode
          mkdir -p dist/duckdb
          curl -L https://install.duckdb.org/v1.4.4/duckdb_cli-osx-universal.zip -o duckdb.zip
          unzip duckdb.zip -d dist/duckdb/
          mv dist/duckdb/duckdb dist/duckdb/duckdb-universal-apple-darwin
          chmod +x dist/duckdb/duckdb-universal-apple-darwin
          rm duckdb.zip
          echo "Downloaded DuckDB CLI for macOS"

      - name: Build CLI for macOS
        run: |
          cd packages/opencode
          bun run build --single
        env:
          OPENCODE_VERSION: "1.2.21"

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-cli-macos
          path: packages/opencode/dist

  build-cli-windows:
    runs-on: windows-latest
    if: inputs.target == 'both' || inputs.target == 'windows-x64'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install

      - name: Download DuckDB CLI for Windows
        run: |
          cd packages/opencode
          mkdir -p dist/duckdb
          curl -L https://install.duckdb.org/v1.4.4/duckdb_cli-windows-amd64.zip -o duckdb.zip
          unzip duckdb.zip -d dist/duckdb/
          mv dist/duckdb/duckdb.exe dist/duckdb/duckdb-x86_64-pc-windows-msvc.exe
          rm duckdb.zip
          echo "Downloaded DuckDB CLI for Windows"

      - name: Build CLI for Windows
        run: |
          cd packages/opencode
          bun run build --single
        env:
          OPENCODE_VERSION: "1.2.21"

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-cli-windows
          path: packages/opencode/dist

  build-tauri-macos-arm:
    needs: build-cli-macos
    if: inputs.target == 'both' || inputs.target == 'macos-arm64'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri

      - uses: actions/download-artifact@v4
        with:
          name: opencode-cli-macos
          path: packages/opencode/dist

      - name: Prepare sidecar and build frontend
        run: |
          cd packages/desktop
          mkdir -p src-tauri/sidecars
          cp ../opencode/dist/opencode-darwin-arm64/bin/opencode src-tauri/sidecars/opencode-cli-aarch64-apple-darwin
          chmod +x src-tauri/sidecars/opencode-cli-aarch64-apple-darwin
          # Copy DuckDB CLI
          if [ -f ../opencode/dist/duckdb/duckdb-universal-apple-darwin ]; then
            cp ../opencode/dist/duckdb/duckdb-universal-apple-darwin src-tauri/sidecars/
            chmod +x src-tauri/sidecars/duckdb-universal-apple-darwin
            echo "Copied DuckDB CLI"
          fi
          bun x vite build
        env:
          RUST_TARGET: aarch64-apple-darwin

      - name: Modify tauri config to skip beforeBuildCommand
        run: |
          cd packages/desktop/src-tauri
          sed -i.bak 's/"beforeBuildCommand": "bun run build"/"beforeBuildCommand": "echo skip"/g' tauri.conf.json

      - name: Build Tauri
        uses: tauri-apps/tauri-action@v0
        with:
          projectPath: packages/desktop
          args: --target aarch64-apple-darwin --config src-tauri/tauri.conf.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-desktop-macos-arm64
          path: |
            packages/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            packages/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/*.app

  build-tauri-windows:
    needs: build-cli-windows
    if: inputs.target == 'both' || inputs.target == 'windows-x64'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri

      - uses: actions/download-artifact@v4
        with:
          name: opencode-cli-windows
          path: packages/opencode/dist

      - name: Prepare sidecar and build frontend
        run: |
          cd packages/desktop
          mkdir -p src-tauri/sidecars
          cp ../opencode/dist/opencode-windows-x64/bin/opencode.exe src-tauri/sidecars/opencode-cli-x86_64-pc-windows-msvc.exe
          chmod +x src-tauri/sidecars/opencode-cli-x86_64-pc-windows-msvc.exe || true
          # Copy DuckDB CLI
          if [ -f ../opencode/dist/duckdb/duckdb.exe ]; then
            cp ../opencode/dist/duckdb/duckdb.exe src-tauri/sidecars/duckdb-x86_64-pc-windows-msvc.exe
            chmod +x src-tauri/sidecars/duckdb-x86_64-pc-windows-msvc.exe || true
            echo "Copied DuckDB CLI"
          fi
          bun x vite build
        env:
          RUST_TARGET: x86_64-pc-windows-msvc

      - name: Modify tauri config to skip beforeBuildCommand
        shell: bash
        run: |
          cd packages/desktop/src-tauri
          sed -i 's/"beforeBuildCommand": "bun run build"/"beforeBuildCommand": "echo skip"/g' tauri.conf.json

      - name: Build Tauri
        uses: tauri-apps/tauri-action@v0
        with:
          projectPath: packages/desktop
          args: --target x86_64-pc-windows-msvc --config src-tauri/tauri.conf.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-desktop-windows-x64
          path: |
            packages/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/*.msi
            packages/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/*.exe
            packages/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/*.dll
